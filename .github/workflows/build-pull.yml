---
name: Check pull requests
on:
  pull_request:
    branches-ignore:
      - '1.x'

concurrency:
  group: "check-pull-request-${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [
          { 'version': '11', opts: '' },
          { 'version': '17', 'opts': '' },
          { 'version': '21', 'opts': '' }
        ]
    name: Build with Java ${{ matrix.java.version }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        name: set up jdk ${{matrix.java.version}}
        with:
          distribution: temurin
          java-version: ${{matrix.java.version}}
          cache: maven
      - name: Build with Maven
        env:
          MAVEN_OPTS: ${{ matrix.java.opts }}
        run: ./mvnw --no-transfer-progress -s .build/maven-ci-settings.xml -B clean verify -Pcoverage
      - name: Codecov
        uses: codecov/codecov-action@v1.0.13

  conventional-commits:
    runs-on: ubuntu-latest
    name: Check conventional commits
    steps:
      - uses: actions/checkout@v3
      - uses: wagoid/commitlint-github-action@v5

  compatibility:
    runs-on: ubuntu-latest
    name: Compatibility Check
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: 11
          distribution: temurin
          cache: maven
      - name: Compatibility Check
        run: ./mvnw --no-transfer-progress -s .build/maven-ci-settings.xml -pl '!bom' -B install revapi:check@check-compatibility -DskipTests -fae

  native-compilation:
    runs-on: ubuntu-latest
    name: Native compilation checks
    steps:
      - uses: actions/checkout@v3
      - uses: sdkman/sdkman-action@v1
        with:
          candidate: java
          version: 21-graalce
      - name: Quick build
        run: ./mvnw --no-transfer-progress -s .build/maven-ci-settings.xml -Dquickly
      - name: Run native tests
        run: ./mvnw --no-transfer-progress -s .build/maven-ci-settings.xml verify -f native-tests -Pnative
